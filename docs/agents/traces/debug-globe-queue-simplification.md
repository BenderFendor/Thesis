# Debug, Globe, And Reading Queue Simplification Trace

Goal: refactor three oversized frontend pages by feature ownership while preserving every existing user-facing capability.
Done criteria: split debug tabs, simplify globe state/view composition, simplify reading queue ownership, preserve behavior, run targeted checks and route smoke tests, and update project records.
Status: implementation and focused/full frontend verification complete; repository-wide self-test timed out inside `verify.sh`.
Files changed: `frontend/app/debug/page.tsx`, `frontend/app/debug/debug-dashboard.tsx`, `frontend/app/debug/debug-tab-navigation.tsx`, `frontend/components/globe-view.tsx`, `frontend/components/globe-ui-state.ts`, `frontend/components/globe-view-model.ts`, `frontend/components/reading-queue-sidebar.tsx`, `frontend/components/reading-queue-article-detail.tsx`, `frontend/components/reading-queue-list.tsx`, `frontend/components/reading-queue-card.tsx`, `frontend/components/reading-queue-digest.tsx`, `frontend/components/reading-queue-embeds.tsx`, `frontend/components/reading-queue-queries.ts`, `frontend/lib/reading-queue-content.ts`, `docs/Log.md`, and `docs/agent/learnings.md`.
Commands and tests: `scripts/agent-summary` passed; frontend TypeScript passed; focused Oxlint passed for all changed debug, globe, and queue modules; five affected frontend suites passed (22 tests); the full frontend suite passed (51 suites, 176 tests); `npm --prefix frontend run build` passed; browser smoke rendered `/debug` with `Debug Console`, `Storage`, and `System`; `git diff --check` passed; full frontend lint failed on repository scripts/tests and existing monolith diagnostics; `scripts/self-test` timed out after 300 seconds while `verify.sh` was running.
Tests added: none; existing behavior suites cover queue, globe workspace, live data, and interactive globe behavior.
Assumptions: the three attached refactor descriptions are implementation requirements for debug, globe, and reading queue; UI behavior and visual language remain unchanged; backend APIs remain unavailable in local smoke, so loading-state rendering is the exercised root-surface behavior.
Risk tier: high; state and async boundaries moved across feature modules, including debug resource queries, globe selection/actions/display derivation, and queue extraction/digest boundaries.
Rollback: revert only files introduced or changed for these three routes; preserve all pre-existing worktree changes.

Audit: the debug route is now a thin Suspense shell with data-driven tab navigation, schema-derived local response types, isolated draft-filter hooks, and independent TanStack Query resource loading plus mutation-backed parser/cache actions. The globe view keeps one rendering surface, with selection/actions and display derivation in `globe-ui-state.ts` and `globe-view-model.ts`; layout prop assembly is local to the content component. The reading queue delegates article extraction, analysis/source/debug/digest queries, detail rendering, list/header rendering, embeds, compact queue cards, and queue selection/modal state to focused modules; queue presentation uses Tailwind classes instead of inline style constants.
Decision: use focused feature boundaries rather than one file per visual fragment. Preserve existing API calls and rendered content while moving ownership to the smallest module that already owns the behavior.
Limitations: the requested approximate line-count budgets are not all met: `debug-dashboard.tsx` remains 3,153 lines and `globe-view.tsx` remains 2,503 lines. The queue sidebar is 409 lines, with detail/list behavior in `reading-queue-article-detail.tsx` (1,050 lines) and `reading-queue-list.tsx` (300 lines). The two large view files still contain pre-existing lint debt; the changed modules pass scoped TypeScript/Oxlint checks.
