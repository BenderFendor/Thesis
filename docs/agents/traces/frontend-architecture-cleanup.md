# Frontend Architecture Cleanup Trace

Goal: reduce high-debt frontend modules and converge duplicated article, image, date, cache, hook, wiki, and API contracts without changing user-visible behavior.
Done criteria: shared primitives are used by affected callers; large routes/components are split at behavior boundaries; focused checks and runtime smoke cover changed paths; docs status and remaining risk are recorded.
Status: implementation complete. Focused checks, the frontend build, and browser route smoke passed. Repository-wide quality closure remains blocked by the documented hardening failures.
Files changed: backend canonical article models/serializers, news and semantic-search contracts, RSS catalog helpers, generated OpenAPI, and contract tests; frontend shared image/date/content/reaction/index/detail/globe/queue/wiki/card modules plus decomposed route/component callers; duplication and quality configuration; `docs/Log.md`, `docs/agent/known-errors.md`, and `docs/agent/learnings.md`. `har/pleasework.har` is unrelated user work and remains untouched.
Commands and tests run: `memo check`; `scripts/agent-summary`; frontend TypeScript; full frontend Jest; frontend production build; backend full pytest; targeted backend contract, saved, and RSS tests; backend `py_compile`; changed-backend Ruff; OpenAPI export/types; duplication scan; quality-controller tests/validation; browser route smoke; `git diff --check`.
Tests added: frontend article-image, date-formatters, globe-workspace, and semantic-search behavior tests; backend canonical article contract coverage; existing render-loop and API mapping tests updated for shared contracts.
Assumptions and risks: canonical browse, cache, and semantic-search responses intentionally remove legacy aliases; the legacy RSS `NewsArticle` model and loose mapper remain only at cache/stream/AI boundaries; saved reaction list models match the flat route payload; the backend was unavailable during browser smoke, so only frontend loading and failure/empty states were exercised. Cross-cutting contract changes remain high risk despite passing focused checks.
Risk tier: high, cross-cutting frontend and backend refactor.
Rollback: revert only this task's tracked and intentional untracked files after reviewing the diff; do not touch `har/`.
Remaining failures or blockers: `scripts/self-test` exceeded its 600-second watchdog in `verify.sh`; changed-scope quality verification reports one CCCC hard violation, 1,540 Oxlint errors, 3,047 Oxlint warnings, CLI typecheck diagnostics including `scripts/scoop.ts:845`, and one CLI test failure. The pinned `oxlint-tsgolint@7.0.2001` dependency is installed and the direct type-aware Oxlint probe now starts successfully.
Next executable step: run a dedicated quality-hardening pass for the exact blockers above; no cleanup work remains in this task.
