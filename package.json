{
  "scripts": {
    "openapi:export": "cd backend && UV_CACHE_DIR=../.uv-cache uv run python -c \"import json; from pathlib import Path; from app.main import app; Path('openapi.json').write_text(json.dumps(app.openapi(), indent=2) + '\\n')\"",
    "openapi:types": "npm --prefix frontend run openapi:types",
    "openapi:refresh": "npm run openapi:export && npm run openapi:types",
    "deps:cycles": "bash ./scripts/check-cycles"
  },
  "devDependencies": {
    "@types/d3-geo": "^3.1.0",
    "@types/three": "^0.180.0"
  },
  "dependencies": {
    "d3-geo": "^3.1.1",
    "framer-motion": "^12.35.1"
  }
}
